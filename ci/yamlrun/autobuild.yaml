- deps:
  - cd src/deps/catkin
  - build:
    - catkin config --extend=/opt/ros/install
    - catkin build --no-status

- lgmath:
  - cd src
  - build:
    - catkin build --no-status
  - test:
    - cd ../devel/lgmath/lib/lgmath
    - ./lgmath_unit_tests
